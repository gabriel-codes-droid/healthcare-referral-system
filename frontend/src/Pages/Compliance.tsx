import { useEffect, useState } from 'react';
import { ShieldCheck } from 'lucide-react';
import { api } from '../services/api';
import { useAuth } from '../context/AuthContext';
import type { AuditLog } from '../Types';
export default function Compliance() { const { user } = useAuth(); const [logs, setLogs] = useState<AuditLog[]>([]); const [error, setError] = useState(''); useEffect(() => { if (user?.role === 'admin') api.getAuditLogs().then(setLogs).catch(e => setError(e.message)); }, [user]); if (user?.role !== 'admin') return <section className="panel"><h1>Compliance</h1><p>Only administrators can review audit information.</p></section>; return <><div className="page-header"><div><h1>Privacy & compliance</h1><p>Administrative controls for access accountability and patient data rights.</p></div></div><section className="panel"><div className="panel-header"><h2><ShieldCheck size={18}/> Security checklist</h2></div><p>Audit trail, patient export, deletion controls, authenticated API access, and offline sync queue are enabled.</p><p><strong>Deployment action required:</strong> configure MongoDB Atlas encryption, restricted network access, backups/retention, and a production JWT secret before handling real health records.</p></section><section className="panel"><div className="panel-header"><h2>Recent audit events</h2></div>{error ? <p className="form-error">{error}</p> : <div className="table-wrap"><table><thead><tr><th>When</th><th>Actor</th><th>Action</th><th>Record</th></tr></thead><tbody>{logs.map(log => <tr key={log.id}><td>{new Date(log.createdAt).toLocaleString()}</td><td>{log.actorName || 'System'}</td><td>{log.action}</td><td>{log.entityType} {log.entityId}</td></tr>)}{!logs.length && <tr><td colSpan={4} className="empty-cell">No audit events yet.</td></tr>}</tbody></table></div>}</section></>; }
